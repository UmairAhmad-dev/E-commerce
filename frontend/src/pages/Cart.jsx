import CartItems from '../components/CartItems/CartItems';

const Cart = () => {
  return (
    <main className="cart-checkout-workspace-canvas" style={{ minHeight: '70vh', background: '#ffffff' }}>
      <CartItems />[cite: 18]
    </main>
  );
};

export default Cart;